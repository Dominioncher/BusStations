apiVersion: apps/v1
kind: Deployment
metadata:
  name: bus-stations-production
spec:
  replicas: 3
  selector:
    matchLabels:
      app: bus-stations-production
  template:
    metadata:
      labels:
        app: bus-stations-production
    spec:
      containers:
        ports:
        - containerPort: 5000
